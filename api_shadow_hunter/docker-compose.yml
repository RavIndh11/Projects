version: '3.8'
services:
  api_shadow_hunter:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env.example
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    networks:
      - shadow_net

networks:
  shadow_net:
    driver: bridge
