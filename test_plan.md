1. **Explore codebase and identify micro-UX improvements**
   - Look through UI templates to find a simple, high-impact UX enhancement.
2. **Implement character count indicator**
   - Add a character count and `maxlength` to the prompt `textarea` in `llm_prompt_guard/app/templates/index.html` to provide real-time user feedback on length limits.
3. **Verify the changes**
   - Use `run_in_bash_session` to check if syntax is valid and tests pass.
4. **Create Journal Entry**
   - Add a journal entry in `.jules/palette.md` for this micro-UX insight.
5. **Pre-commit Steps**
   - Execute pre-commit instructions to ensure testing, verifications, reviews and reflections are done.
6. **Submit PR**
   - Push code and create a PR with the required details.
